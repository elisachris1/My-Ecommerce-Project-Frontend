import { Historycolletionvalidators } from './historycolletionvalidators';

describe('Historycolletionvalidators', () => {
  it('should create an instance', () => {
    expect(new Historycolletionvalidators()).toBeTruthy();
  });
});
